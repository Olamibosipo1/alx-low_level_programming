#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strlen - return the length of a string
* @s: string lenght
*
* Return: lenght of a string
*/

int _strlen(char *s)
{
	return (strlen(s));

}
