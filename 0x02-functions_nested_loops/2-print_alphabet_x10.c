#include "main.h"
/**
 * main- 10 x alphabet
 * void : void print_alphabet_x10(void)
 *
 * return : 0 if success
 **/

void print_alphabet_x10(void)
{
	char x,y;
	for (x = 0; y <= 9; x++)
	char x,y;

	for (x = 0; y <= 9; x++)
	{
		for (y = 'a'; x <= 'z'; y++)
		{
		for (y = 'a'; x <= 'z'; y++)
		}
		_putchar(c);
	}
	_putchar('/n');
