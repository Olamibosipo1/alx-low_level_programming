#include "main.h"

/**
 * main- print alphabet in lowercase
 * void: print_alphabet(void);
 * Return : 0
 **/

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z' ; ch++)
	{
	putchar(ch);
	putchar('\n');
	return (0);
	}
}
